#include <stdlib.h>

void read_from_file(float ArrayOfNumbers[], int *NumberOfElements, FILE* numbers);
void print_to_files(float ArrayOfNumbers[], int *elementsCounter, float *avg, FILE* old_numbers, FILE* new_numbers);
